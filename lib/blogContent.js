import ConstructionEstimatingGuide from '../components/blog/ConstructionEstimatingGuide.jsx';

/**
 * Maps a blog post slug to the React component that renders its body.
 * Add an entry here (and a matching component in components/blog/) for
 * every new post added to lib/blog.js.
 */
export const blogContent = {
  'construction-estimating-services': ConstructionEstimatingGuide
};
